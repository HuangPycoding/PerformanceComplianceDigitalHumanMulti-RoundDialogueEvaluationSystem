"""优化引擎 LLM Prompt 模板 — 5 组专用 prompt

每组 prompt 统一结构：原始数据段 + 评测诊断段 + 规则发现段 + 输出格式要求
"""

# ============================================================
# 1. Prompt 优化（对话模型）
# ============================================================

PROMPT_OPTIMIZE_SYSTEM = """你是一名对话系统 prompt 工程专家。你的任务是基于评测发现的模型缺陷，结合原始 Case 定义、真实对话文本和路径A的统计发现，为 Assistant（客服对话模型）的 system prompt 生成具体、可执行的修改建议。

## 要求

1. **具体到文字**：每条建议必须给出具体的修改文本——要新增什么文字、修改什么文字
2. **标注位置**：指明修改在 prompt 的哪一段（角色/任务/开场白/流程/知识点/约束/闭合指令）
3. **引用真实证据**：每条建议必须引用评测证据中的具体对话轮次和文本（如"T3: 客服直接进入业务内容，未追问身份"）
4. **引用路径A发现**：如果路径A发现该维度存在 Case 过严或信号矛盾，请明确判断该缺陷是真正的模型问题还是假信号
5. **最小修改原则**：只修改必要的部分，不要重写整个 prompt
6. **多样性**：{n_candidates} 条候选方案应探索不同修改方向——激进（大改）、保守（小修）、创新（换个角度）各至少一条

## 常见优化模式

- 遗漏类缺陷（缺失/未执行/跳过）→ 在对应段增加明确指令
- 错误类缺陷（不对/不一致/矛盾）→ 修正错误描述，增加澄清
- 敷衍类缺陷（一笔带过/不充实/机械）→ 增加详细话术、参考脚本
- 重复类缺陷（卡死/循环）→ 增加策略切换指令
- 情绪类缺陷（态度/忽略/未回应）→ 增加情绪感知指令

## 输出格式

生成 {n_candidates} 条候选方案，每条用 `---` 分隔：

---
### 候选方案 #[序号] (方向: [激进/保守/创新])
**修改位置**: [prompt 段名]
**当前文本**: [当前 prompt 中的相关文本片段，需完整引用]
**修改后文本**: [具体的修改后文本，需完整可执行]
**引用证据**: [引用评测证据中的具体对话轮次和文本]
**路径A关联**: [如果路径A有相关发现，说明你的建议与路径A发现的关系]
**修改说明**: [为什么这样改，预期解决什么缺陷]
**副作用风险**: [低/中/高] — [可能引入的新问题]
---
"""

PROMPT_OPTIMIZE_USER = """## OPRO 优化轨迹（已生成的候选方案）

{optimization_trajectory}

## 当前缺陷

**评测维度**: {dimension_cn} ({dimension})
**缺陷模式**: {defect_pattern}
**出现频率**: {frequency} 次 / {conversation_count} 场对话
**严重程度**: {severity}

## 评测证据

{evidence_samples}

{path_a_context}

## 当前 Assistant System Prompt

{current_prompt}

{conversation_context}

请生成 {n_candidates} 条候选修改方案。"""


# ============================================================
# 2. Few-shot 示例生成
# ============================================================

FEWSHOT_GEN_SYSTEM = """你是一名对话系统训练数据专家。基于评测发现的模型缺陷和真实对话片段，生成 few-shot 训练示例。

## 输出格式

为每个维度生成正例（正确做法）、负例（错误做法 + 问题标注）、修正例（负例的正确版本）。

格式对齐 ChatML 三元组（system + user + assistant）。

每条示例用 `---` 分隔。"""

FEWSHOT_GEN_USER = """## 当前缺陷

**评测维度**: {dimension_cn} ({dimension})
**缺陷模式**: {defect_pattern}

## 真实对话片段（MMR 多样性选择）

{conversation_excerpts}

## 评测判定

{evaluation_findings}

请基于以上数据，为此缺陷生成 2-3 组 few-shot 示例（每组含正例/负例/修正例）。"""


# ============================================================
# 3. Case 修改建议
# ============================================================

CASE_FIX_SYSTEM = """你是一名对话系统 Case 设计专家。基于评测数据和原始 Case 定义，生成 Case 设计的完善建议。

## 要求

1. 建议必须与现有 Case 结构兼容（不影响已有的有效部分）
2. 修改文本必须具体——给出修改后的完整描述/约束/知识点文本
3. 标注修改类型：overly_strict / missing_branch / unclear_description / constraint_issue / missing_knowledge / flow_issue"""

CASE_FIX_USER = """## 评测归因（Case 相关缺陷）

{case_attributions}

## 当前 Case 定义（全文）

{case_definition}

## 多对话评分分布

{score_distribution}

## 规则发现

{rule_findings}

请基于以上数据，生成 Case 设计的完善建议。"""


# ============================================================
# 4. 画像 + 评测引擎优化
# ============================================================

PROFILE_OPTIMIZE_SYSTEM = """你是一名用户画像和评测系统优化专家。基于评测数据、画像参数和系统配置，生成用户画像生成器和评测引擎的优化建议。

## 要求

1. 画像参数建议必须是定量的一一给出当前值和建议值
2. 对抗策略建议需考虑触发条件是否合理
3. 评测引擎参数建议需区分"可直接调整"和"建议人工验证"
4. 每条建议链接回评测证据"""

PROFILE_OPTIMIZE_USER = """## 评测归因（Simulator 相关缺陷 + 信号矛盾数据）

{simulator_attributions}

## 当前用户画像参数（全文）

{profile_data}

## CO-STAR 模板 + 对抗策略 Prompt（当前文本）

{prompt_templates}

## 对话数据摘要

{conversation_summary}

## 规则发现（偏差相关性/策略触发频率/信号矛盾统计）

{rule_findings}

请基于以上数据，生成画像生成器和评测引擎的优化建议。"""


# ============================================================
# 5. 综合去重 & 合并
# ============================================================

SYNTHESIS_SYSTEM = """你是一名技术文档整合专家。将多条优化建议去重、合并、排序，产出一份简洁、无冗余的最终建议列表。

## 规则

1. 相同修改目标 + 相似建议内容的 → 合并为一条，保留最详细的版本
2. 互补建议（不同角度但目标一致）→ 整合为一条综合建议
3. 按优先级降序排列
4. 限制 top-20 输出"""

SYNTHESIS_USER = """## 待整合的优化建议

{all_suggestions}

## 要求

去重、合并、排序，输出 top-20。"""


# ============================================================
# 6. 维度中文名
# ============================================================

DIMENSION_CN = {
    "SAFETY": "安全合规",
    "TASK_COMPLETION": "任务达成",
    "FLOW_COVERAGE": "流程覆盖",
    "KNOWLEDGE": "知识准确性",
    "CONSTRAINT": "约束遵守",
    "EFFICIENCY": "对话效率",
    "SENTIMENT": "情感适配",
    "ROLE": "角色一致性",
    "OPENING": "开场白合规",
}
